# Python files
*.pyc
__pycache__/

# Django stuff:
db.sqlite3
*.sqlite3*
media/
# collectstatic output (STATIC_ROOT). Scoped to the project root so the app's
# static *source* (myocyte/toxtempass/static/, incl. the compiled bootstrap.css)
# stays tracked.
/myocyte/static/
# Autogenerated stuff:
logs/

# Node (frontend build: Dart Sass compiles scss/theme.scss -> committed CSS)
node_modules/

# document binary files
*.pdf
*.docx

# Virtual environment:
env/
venv/
.venv/

# Node / SCSS build deps (only the compiled theme.css is committed; see myocyte/scss/theme.scss)
node_modules/

# misc
.DS_Store
.env
models_diagram.png
/*.png
/*/*.png 
!/*/*/*/*.png
.vscode

# Files for VHP4Safety instant
docker-compose-vhp4safety.yml
.env-vhp4safety
docker-compose-MacOs.yml

# Analysis files
data_analysis/

# temp file
change_toxtemp.md

# testings
.coverage
.coverage.*
*.coveragerc

# pytest cache
.pytest_cache/
# Ruff cache
.ruff_cache/ 

# Queue storage
__queuestorage__/

# evaluation outputs
output/

#backups
backups/
.claude/

# Real-world evaluation input documents (unpublished data).
# Ignore every document but keep the folder structure tracked via .gitkeep files.
myocyte/toxtempass/evaluation/real_world/input_files/**
!myocyte/toxtempass/evaluation/real_world/input_files/**/
!myocyte/toxtempass/evaluation/real_world/input_files/**/.gitkeep

# gold_standard outputs (gold answers + reviewer emails = PII): ignore the data,
# keep the folder tracked via .gitkeep.
!myocyte/toxtempass/evaluation/gold_standard/output/
myocyte/toxtempass/evaluation/gold_standard/output/**
!myocyte/toxtempass/evaluation/gold_standard/output/.gitkeep
skills-lock.json
CLAUDE.md
CLAUDE.md
