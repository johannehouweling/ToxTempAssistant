name: toxtempass
services:
  postgres_for_django:
    image: postgres:17-alpine
    container_name: postgres_for_django
    restart: unless-stopped
    env_file:
      - ./.env
    expose:
      - 5432
    network_mode: host
    volumes:
      - db_data:/var/lib/postgresql/data
  djangoapp:
    build: 
      context: ./myocyte
      dockerfile: dockerfiles/djangoapp/Dockerfile
    user: root
    container_name: djangoapp
    restart: unless-stopped
    command: gunicorn myocyte.wsgi:application --bind 0.0.0.0:8000 --log-level=info --max-requests=1000 --timeout=120 --keep-alive=5 --graceful-timeout=120 --error-logfile=/home/myocyte/logs/gunicorn-error.log --access-logfile=/home/myocyte/logs/gunicorn-access.log --workers=3
    volumes:
      - ./myocyte/logs:/home/myocyte/logs
      - ./myocyte/static:/home/myocyte/static
      - ./myocyte/media:/home/myocyte/media
    network_mode: host
    depends_on:
      - postgres_for_django
    env_file:
      - ./.env
volumes:
  db_data:
    