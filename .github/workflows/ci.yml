name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker
        uses: docker/setup-docker-action@v4

      - name: Build and Test in Docker
        run: |
          echo "🔨 Starting Docker compose tests..."
          if docker-compose up --abort-on-container-exit --exit-code-from djangoapp; then
            echo "✅ Tests passed!"
            exit 0
          else
            echo "❌ Tests failed!"
            exit 1
          fi

          # Figure out which container ran djangoapp
          CONTAINER_ID=$(docker compose ps -q djangoapp)
          echo "📦 Copying test-results from container $CONTAINER_ID …"
          mkdir -p test-results
          docker cp "$CONTAINER_ID":/tmp/test-results/. ./test-results/ || echo "⚠️ No test-results directory found in the container."

          # Exit with the original pytest exit code so the job still fails if tests failed
          exit $code
        env:
          TESTING: "true"

      - name: Upload test results
        if: always() # even on failure
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: test-results/
